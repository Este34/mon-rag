# Configuration Ruff — linter + formateur Python
line-length = 100
target-version = "py311"

exclude = ["__pycache__", ".venv", "venv"]

[lint]
select = ["E", "W", "F", "I", "UP", "B"]
ignore = ["E501"]

[format]
quote-style = "double"
indent-style = "space"
